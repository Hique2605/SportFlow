info:
  name: 02 - Listar usuarios
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/usuarios
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("retorna 200 e uma lista", function () {
          expect(res.status).to.equal(200);
          expect(res.body).to.be.an("array");
        });
        test("a lista contem o usuario criado", function () {
          expect(res.body.some(usuario => usuario.id === bru.getVar("usuarioId"))).to.equal(true);
        });

settings:
  encodeUrl: true
