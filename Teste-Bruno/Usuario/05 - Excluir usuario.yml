info:
  name: 05 - Excluir usuario
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/usuarios/91d27c6d-c901-4970-ac66-82e611b2c1e5
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("retorna 204 sem conteudo", function () {
          expect(res.status).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
